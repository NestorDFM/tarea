Taller 3 .
NESTOR FERREIRA
 Aplicaciones web

	1. Que es el servidor?
  	  Es un programa que es capaz de atender solicitudes y enviar una respuesta

	2. Que es un protocolo dentro de la informática?
 	  Es un conjunto de reblas que permite la comunicación entre entidades 
	
        3. Que es un servidor web
 	  Es un programa que es capaz de atender requisiciones y enviar una respuesta utilizando  el protocolo HTTP
	
        4. Que es un cliente?
 	  Es el que hace una petición al servidor

	5. Cual es el cliente web por excelencia?
                   Las aplicaciones, navegadores web
	6. Que es HTML?

 	  Es un lenguaje de marcación que utiliza etiquetas para poder estructurar  un documento
	7. Que es un lenguaje de programación? 
	   Es un interfaz entre usuario y maquina que permite traducir el lenguaje del usuario a la maquina 
	Debe de contener:  
 	  * Variable
 	  * Elemento de expresión 																								                        Otros
 																																1. Tin Berners-Lee creador del protocolo Http
 																																Hipertexto
          Los navegadores son estándares

        8. Que son las etiquetas?
          Un estructura que permite marcar un valor Ejemplo:<i> o </i>
	EJ:<html> o </html>
         Patron componet container son etiquetas que contienen a otras 
        8.1 Cuales son las diferencias entre etiquetas h1-h6?
	Estas etiquetas son utilizadas para los títulos \subtítulos de un texto, van decreciendo en tamaño a medida que los números que acompañan a la hache (h) aumenta.
        9. Con que etiqueta se agrega una imagen en un documento HTML?
	Se agrega con <img src=” ”>entre comillas va el origen donde se ubica la imagen.
	10. Con que etiqueta se agrega un video  en un documento HTML?
	Se agrega con [<video src=” ”></video>] el source del video va insertado dentro  de las  comillas 
	11 .Con que etiqueta se agrega una sonido en un documento HTML?
        Se agrega con [<audio src=”aquí va la ubicación”></audio>]
	12. Con que atributos se indica la imagen que se utiliza dentro de una etiqueta de imagen?
        Se indica con el atributo src=”” que en otras palabras es  source o fuente.
	13. Como se indica un navegador que el documento está escrito  en idioma ingles
          Al iniciar la apertura de la etiqueta HTML se escribe el siguiente comando <html lang=”en”>
	14. Que es una lista ordenada?
	Una lista ordenada o <o|></0|> es una etiqueta que ordena con números un determinado texto rodeado por estas etiquetas  
	15. Que es una lista no ordenada?
         Una lista no ordenada o <ul/></ul> es una lista que en vez de utilizar números utiliza viñetas, de vuelta se convierte en lista cada texto rodeado con estas  etiquetas 
	17. Como se indica un párrafo?
	Con la etiqueta <p> el párrafo aquí </p>
	18 .Como se indica la negrita la cursiva, el subrayo, el tachado, el subíndice y el superíndice?
            Negrita se utiliza <strong>Aquí el texto en negrita</strong>
	    Cursiva se utiliza la etiqueta <em></em>
	    Subrayo se utiliza <u></u>
	    Subíndice se utiliza <sub></sub>
	    Tachado se utiliza <strike></strike>
	    Superíndice se utiliza <sup></sup>
	19. Que es git?
	    Git es un software de control de versiones o en otras palabras se utiliza para programar de manera ordenada ya sea individualmente o en equipo  
	20. Que es un github?
	    Github es un repositorio que aloja proyectos de desarrollo colaborativo 
	21. Que es clonar un repositorio, cual comando se utiliza?
	    Es copiar todo el repositorio remoto o alojado en la nube a tu maquina se utiliza el comando git clone [url de lo que se desea clonar].
        22. Que es un commit?
            Un commit es un comando utilizado para guardar el archivo en el repositorio
	23. Que es un push?
	    Es enviar tu archivo  a repositorios remotos alojados en la nube 
	24. Que es la rama master ?
	    Es la rama por defecto (default) de git.
	25.Que es un repositorio local y remoto?
		Un repocitorio remoto es el archio que esta el la nube y el local esta en la memoria de la computadora  
	26.cual es la función de la etiqueta doctype?
	    Específica al navegador tipo de archivo pueden ser html,css,etc.

